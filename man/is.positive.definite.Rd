% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/portfolio_risk_simulation.R
\name{is.positive.definite}
\alias{is.positive.definite}
\title{Monte Carlo Portfolio Risk Simulation with Stress Testing}
\usage{
is.positive.definite(x)
}
\arguments{
\item{historical_returns}{A data frame, tibble, or matrix of asset returns.
One column per asset, one row per observation period. An optional date
column (class \code{Date}, \code{POSIXct}, or character coercible to \code{Date}) is
used to infer return frequency automatically.}

\item{weights}{One of: \code{NULL} (equal weights, default); a named numeric
vector summing to 1; a named list with asset names or 1-based column
indices as keys; or a data frame / matrix where each row is a distinct
weight allocation.}

\item{sim_returns_dist}{Character. Marginal return distribution.
One of \code{"rmvnorm"} (Gaussian), \code{"rmvt"} (Student-t), or \code{"rsgt"}
(Skewed Generalised-t). Default \code{"rmvnorm"}.}

\item{cov_estimation}{Character. Forward-looking covariance method.
One of \code{"sample"}, \code{"ewma"}, or \code{"garch"}. Default \code{"sample"}.}

\item{cov_lambda}{Numeric in (0, 1). EWMA decay parameter. Only used
when \code{cov_estimation = "ewma"}. Defaults to \code{0.97} if \code{NULL}.}

\item{calibrate_params}{Logical. If \code{TRUE}, fits distribution shape
parameters (degrees of freedom for \code{rmvt}; lambda, p, q for \code{rsgt})
to each asset's historical returns. Default \code{FALSE}.}

\item{calibration_method}{Character. Moment-matching strategy when
\code{calibrate_params = TRUE}. One of \code{"tail"} (match excess kurtosis only)
or \code{"full"} (match all four moments equally). Default \code{"tail"}.}

\item{asset_dfs}{Numeric vector. Per-asset degrees of freedom for \code{rmvt}
when \code{calibrate_params = FALSE}. Length must equal number of assets.}

\item{asset_lambdas}{Numeric vector. Per-asset skewness parameters for
\code{rsgt}. Values in (-1, 1).}

\item{asset_p}{Numeric vector. Per-asset peakedness parameters for \code{rsgt}.}

\item{asset_q}{Numeric vector. Per-asset tail-decay parameters for \code{rsgt}.}

\item{copula}{Logical. If \code{TRUE}, fits a copula to the PIT residuals and
uses it to model dependence separately from the marginals. Default \code{FALSE}.}

\item{copula_type}{Character. Copula family. One of \code{"gaussian"} or \code{"t"}.
Only used when \code{copula = TRUE}. Default \code{"gaussian"}.}

\item{copula_df}{Numeric. Starting degrees of freedom for the t-copula.
Only used when \code{copula = TRUE} and \code{copula_type = "t"}. Default \code{4}.}

\item{n_sim_returns}{Integer. Number of simulation draws per weight set.
If \code{NULL} (default), uses \code{nrow(historical_returns)}.}

\item{propagation}{Logical. If \code{TRUE} (default), return shocks are
propagated to unshocked assets via the Schur complement conditional
mean formula. If \code{FALSE}, each shocked asset is modified in isolation.}

\item{asset_shock}{Named list or named numeric vector. Return shock
(decimal) for each endogenous asset. Unspecified assets default to 0.
Accepts names or 1-based column indices as keys.}

\item{volatility_shock}{Named list or named numeric vector. Volatility
multiplier for each endogenous asset. Unspecified assets default to 1
(no change). Accepts names or 1-based column indices as keys.}

\item{exogenous_returns}{A data frame of returns for non-portfolio
instruments (e.g. SPY, Crude Oil). Must include a date column aligned
with \code{historical_returns}. These instruments carry zero portfolio weight
and are used as shock transmission channels only.}

\item{exogenous_shock}{Named list or named numeric vector. Return shock
(decimal) for each exogenous instrument. Propagated to all portfolio
asset means via \code{Sigma_pe \%*\% solve(Sigma_ee) \%*\% delta_e}.}

\item{exogenous_volatility_shock}{Named list or named numeric vector.
Volatility multiplier for each exogenous instrument. Widens portfolio
covariance via the Law of Total Variance addon.}

\item{df}{Numeric. Global degrees of freedom for \code{rmvt} when
\code{calibrate_params = FALSE}. Default \code{4}.}

\item{seed}{Integer. Random seed for reproducibility. Default \code{123}.}

\item{reference_asset}{Character or integer. The asset shown on the
y-axis of the pairwise latent-space diagnostic scatter plots.}

\item{n_pairwise_plots}{Integer. Maximum number of asset pairs to plot
in the pairwise diagnostics panel. Default \code{12}.}
}
\value{
A named list with the following components:
\describe{
\item{\code{portfolio_stats}}{14-row data frame: Metric, Pre_Stress, Post_Stress.}
\item{\code{asset_draws}}{\verb{n_sim x n_assets} data frame of pre-stress per-asset draws.}
\item{\code{asset_draws_stress}}{\verb{n_sim x n_assets} data frame of post-stress draws; \code{NULL} if not stressed.}
\item{\code{asset_empirical_summary}}{Per-asset empirical mean, SD, skewness, kurtosis, min, max.}
\item{\code{exogenous_asset_empirical_summary}}{Empirical summary for exogenous instruments; \code{NULL} if none.}
\item{\code{exogenous_historical_correlation}}{Portfolio x exogenous Pearson correlation data frame.}
\item{\code{ks_test}}{KS uniformity test results: Asset, KS_stat, p_value, Flag.}
\item{\code{stress_impact_summary}}{7-row data frame: Metric, Pre_Stress, Post_Stress, Change.}
\item{\code{stressed_covariance}}{n x n stressed covariance matrix after shock application.}
\item{\code{stressed_means}}{Named vector of stressed conditional means per asset.}
\item{\code{final_covariance}}{n x n final forward-looking covariance matrix.}
\item{\code{copula_correlation}}{Fitted copula correlation matrix; \code{NULL} if \code{copula = FALSE}.}
\item{\code{tail_dependence}}{Tail dependence matrix; \code{NULL} if Gaussian copula or \code{copula = FALSE}.}
\item{\code{calibrated_params}}{Calibrated shape parameters per asset; \code{NULL} if not calibrated.}
\item{\code{plots}}{List with \code{portfolio_diagnostics} containing named ggplot/patchwork objects.}
\item{\code{stressed_plots}}{Stress-scenario visualisations; \code{NULL} if no shocks specified.}
}
}
\description{
An end-to-end function for multi-asset portfolio risk modelling.
Given a historical return series, the function estimates the joint
return distribution, constructs a forward-looking covariance matrix,
simulates portfolio return paths, and optionally applies structured
stress scenarios whose effects propagate through the correlation structure.
}
\examples{
\dontrun{
# Minimal call — Gaussian, equal weights, no stress
result <- portfolio_risk_simulation(returns_df)

# Fat-tailed with t-copula and EWMA covariance
result <- portfolio_risk_simulation(
  returns_df,
  sim_returns_dist   = "rmvt",
  cov_estimation     = "ewma",
  cov_lambda         = 0.97,
  calibrate_params   = TRUE,
  copula             = TRUE,
  copula_type        = "t"
)

# Stress: SPY falls 5\%, vol doubles
result <- portfolio_risk_simulation(
  returns_df,
  exogenous_returns          = exo_df,
  exogenous_shock            = list(SPY = -0.05),
  exogenous_volatility_shock = list(SPY = 2)
)
}

}
\references{
Glosten, L.R., Jagannathan, R., & Runkle, D.E. (1993).
On the relation between the expected value and the volatility of the
nominal excess return on stocks. \emph{Journal of Finance}, 48(5), 1779--1801.

Ledoit, O., & Wolf, M. (2004). A well-conditioned estimator for
large-dimensional covariance matrices.
\emph{Journal of Multivariate Analysis}, 88(2), 365--411.

Nelsen, R.B. (2006). \emph{An Introduction to Copulas} (2nd ed.). Springer.

Sklar, A. (1959). Fonctions de repartition a n dimensions et leurs marges.
\emph{Publications de l'Institut Statistique de l'Universite de Paris}, 8, 229--231.

Theodossiou, P. (1998). Financial data and the skewed generalised t distribution.
\emph{Management Science}, 44(12), 1650--1661.
}
\seealso{
Vignettes: \code{vignette("portfolio_risk_simulation_vignette", package = "portfolioRisk")}
and \code{vignette("portfolio_risk_simulation_scenarios", package = "portfolioRisk")}
}
